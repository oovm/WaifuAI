{
    "private": true,
    "scripts": {
        "build": "cargo build --release",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "push": "wee test && git push",
        "reset": "git reset Head~ --soft",
        "u": "cargo upgrade --workspace",
        "cli": "cargo run --package nai-waifu -- -f 8",
        "tag": "git push --prune --tags"
    }
}
